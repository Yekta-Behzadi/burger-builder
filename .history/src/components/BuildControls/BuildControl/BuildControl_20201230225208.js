import React from 'react'
import cla
const BuildControl = (props) => {
  return (
    <div>
      <div>{props.label} </div>
      <button>Less</button>
      <button>More</button>
    </div>
  )
}

export default BuildControl
