import React from 'react'

const Input = (props) => {
  switch (props.) {
    case value:
      
      break;
  
    default:
      break;
  }
  return (
    <div>
      <label> {props.label} </label>
    </div>
  )
}

export default Input
