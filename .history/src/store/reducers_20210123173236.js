import * as actionType from './actions';

initialState={

}

const reducer =(state = initialState, )=> {
  switch (key) {
    case actionType.ADD_INGREDIENT:
      return{

      }
      
      break;
  
    default:
      break;
  }
}