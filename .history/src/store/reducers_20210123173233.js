import * as actionType from './actions';

initialState={

}

const reducer =(state = init)=> {
  switch (key) {
    case actionType.ADD_INGREDIENT:
      return{

      }
      
      break;
  
    default:
      break;
  }
}